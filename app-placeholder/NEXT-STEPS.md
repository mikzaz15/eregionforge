# App Placeholder

This folder exists only as a reminder that the actual app repo should now be created with:
- Next.js
- TypeScript
- Tailwind
- Supabase

Start with the files in `docs/` and `workspace/prompts/`.
